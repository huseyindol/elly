spring.application.name=elly
# spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:postgres}
spring.datasource.url=jdbc:postgresql://ep-billowing-scene-adbekobg-pooler.c-2.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require&connectTimeout=60&socketTimeout=60
spring.datasource.hikari.schema=${DB_SCHEMA:elly}
spring.datasource.username=${DB_USER:neondb_owner}
spring.datasource.password=${DB_PASSWORD:npg_NExeW0baq3HB}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.docker.compose.enabled=false
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.default_schema=elly
# Database indexes - Manual execution required after tables are created
# Run: psql -U postgres -d postgres -f src/main/resources/db-indexes.sql
# spring.sql.init.mode=always
# spring.sql.init.schema-locations=classpath:db-indexes.sql

springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.enabled=true

# File upload configuration
file.upload.directory=assets/images
file.upload.directory.files=assets
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# JWT Configuration
jwt.secret=${JWT_SECRET:mySecretKey123456789012345678901234567890}
# 30 dk
jwt.expiration=1800000
# 60 dk
jwt.refresh.expiration=3600000